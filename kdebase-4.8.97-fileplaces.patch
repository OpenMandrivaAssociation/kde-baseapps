diff -urN kde-baseapps-4.8.97/dolphin/src/panels/places/placesitemmodel.cpp kde-baseapps-4.8.97-patched/dolphin/src/panels/places/placesitemmodel.cpp
--- kde-baseapps-4.8.97/dolphin/src/panels/places/placesitemmodel.cpp	2012-06-26 07:10:42.000000000 +1100
+++ kde-baseapps-4.8.97-patched/dolphin/src/panels/places/placesitemmodel.cpp	2012-07-25 20:57:09.922443705 +1100
@@ -34,6 +34,7 @@
 #include <KLocale>
 #include <KStandardDirs>
 #include <KUser>
+#include <KGlobalSettings>
 #include "placesitem.h"
 #include <QAction>
 #include <QDate>
@@ -900,6 +901,23 @@
                                                 "user-trash",
                                                 I18N_NOOP2("KFile System Bookmarks", "Trash")));
 
+    // We add Documents, Music, Downloads, Pictures, Videos to be in sync with kdelibs
+    m_systemBookmarks.append(SystemBookmarkData(KUrl(KGlobalSettings::documentPath()),
+                                                "folder-documents",
+                                                I18N_NOOP2("KFile System Bookmarks", "Documents")));
+    m_systemBookmarks.append(SystemBookmarkData(KUrl(KGlobalSettings::musicPath()),
+                                                "folder-sound",
+                                                I18N_NOOP2("KFile System Bookmarks", "Music")));
+    m_systemBookmarks.append(SystemBookmarkData(KUrl(KGlobalSettings::downloadPath()),
+                                                "folder-downloads",
+                                                I18N_NOOP2("KFile System Bookmarks", "Downloads")));
+    m_systemBookmarks.append(SystemBookmarkData(KUrl(KGlobalSettings::videosPath()),
+                                                "folder-video",
+                                                I18N_NOOP2("KFile System Bookmarks", "Videos")));
+    m_systemBookmarks.append(SystemBookmarkData(KUrl(KGlobalSettings::picturesPath()),
+                                                "folder-image",
+                                                I18N_NOOP2("KFile System Bookmarks", "Pictures")));
+
     if (m_fileIndexingEnabled) {
         m_systemBookmarks.append(SystemBookmarkData(KUrl("timeline:/today"),
                                                     timeLineIcon,
